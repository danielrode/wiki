# Source script

    %run ./my_script.py

# Dump session history to file

    %hist -f hist.txt

# Print function source

    import inspect
    print(inspect.getsource(FUNC_NAME))

OR

    FUNC_NAME??
